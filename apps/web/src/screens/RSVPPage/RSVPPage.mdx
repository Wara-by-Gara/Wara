import { Meta, Canvas, Title, Subtitle } from "@storybook/addon-docs/blocks";
import * as Stories from "./RSVPPage.stories";

<Meta of={Stories} name="Docs" />

<Title>RSVP</Title>
<Subtitle>참석/미정/불참 응답 + 이름·동반인원·요청·커스텀 질문</Subtitle>

## 상태 다이어그램
```
entry → selectStatus → (attending|maybe|decline) → form 입력
     → confirm → submitLoading → complete
edit ↔ editComplete  (이미 응답한 경우)
closed | fullCapacity | loginRequired | networkError | alreadySubmitted
```

<Canvas of={Stories.SelectStatus} />
<Canvas of={Stories.AttendingSelected} />
<Canvas of={Stories.RequiredQuestionError} />
<Canvas of={Stories.SubmitLoading} />
<Canvas of={Stories.Complete} />
<Canvas of={Stories.Edit} />
<Canvas of={Stories.CancelConfirmModal} />
<Canvas of={Stories.Closed} />
<Canvas of={Stories.FullCapacity} />
<Canvas of={Stories.AlreadySubmitted} />
