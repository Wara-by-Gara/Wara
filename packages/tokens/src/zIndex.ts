/** z-index 레이어 — 오버레이 충돌 방지용 단일 출처. */
export const zIndex = {
  base: 0,
  dropdown: 1000,
  sticky: 1100,
  header: 1200,
  overlay: 1300,
  modal: 1400,
  toast: 1500,
  tooltip: 1600,
} as const;
